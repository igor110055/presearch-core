import("//build/config/features.gni")

source_set("importer") {
  sources = [
    "presearch_importer_p3a.cc",
    "presearch_importer_p3a.h",
  ]

  deps = [
    "//base",
    "//chrome/common/importer",
  ]
}
