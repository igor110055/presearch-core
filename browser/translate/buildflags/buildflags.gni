import("//presearch/build/config.gni")
import("//extensions/buildflags/buildflags.gni")

declare_args() {
  enable_presearch_translate_go = false
}

declare_args() {
  enable_presearch_translate_extension =
      !enable_presearch_translate_go && enable_extensions
}
